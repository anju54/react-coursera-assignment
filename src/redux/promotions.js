import { PROMOTIONS } from "../shared/dishes";
import { actions } from "react-redux-form";

// This is reducer
export const Promotions = (state = PROMOTIONS, actions) => {
    switch(action.type){
        default : 
            return state;
    }
}